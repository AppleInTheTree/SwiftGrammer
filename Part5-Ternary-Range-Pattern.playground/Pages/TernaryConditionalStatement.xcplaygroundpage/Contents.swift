import UIKit

// condition ? print(1) : print(2)
// if condition is true it print 1 else if it is false it print 2

//If a is less then 5 "Ahn" is going to the name else "Park" will go in to name variable
// Ternary conditional statement is not used by only it self, but it is used often when you are sustituting the result
let a : Int = 3
var name = a > 5 ? "Ahn" : "Park"



